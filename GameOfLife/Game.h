#pragma once
#include <iostream>
#include <string>

using namespace std;

class Game {
	int sX, sY;
	int capacityX, capacityY;
	string live, death;
	bool** world;
public:
	void run();
	
	vector<vector<bool>>world(sY; vector<bool>(sX;false))

	int main() {
		
		vector<vector<bool>>world(sY; vector<bool>(sX; bool))
		
			
		bool** world = new bool* [8];
		for (bool i = 0; i < 6; i++)
			world[i] = new bool[i + 1];
	}

};
